"use client";

import { Button } from "@heroui/react";

const HeroUiButton = () => {
  return (
      <Button color="primary" variant="solid">
        Test HeroUI Button
      </Button>
  );
}

export default HeroUiButton;